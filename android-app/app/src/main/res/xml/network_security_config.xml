<?xml version="1.0" encoding="utf-8"?>
<!--
    Android blocks plain HTTP by default, and the development backend runs on
    http://10.0.2.2:8000 without TLS.

    Rather than switching cleartext on for the whole app
    (android:usesCleartextTraffic="true"), it is enabled only for the two hosts
    that can be a local development machine. Everything else stays HTTPS-only,
    so this exception cannot quietly weaken a real deployment.

    10.0.2.2 = the Android emulator's alias for the host machine's 127.0.0.1.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
